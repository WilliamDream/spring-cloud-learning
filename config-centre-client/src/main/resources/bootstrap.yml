
spring:
  application:
    # 此名字要与配置中心文件xxx-profile.yaml 一致
    name: config-client
  cloud:
    config:
      label: master
      profile: test
      uri: http://localhost:10010/
#      discovery:
#        enabled: true
#        service-id: config-server


management:
  security:
    enabled: false




